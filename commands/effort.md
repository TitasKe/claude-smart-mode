You manage claude-smart-mode effort settings.

If `$ARGUMENTS` is empty or `status`, run:

`bash ~/.claude/smart-toggle.sh effort status`

If `$ARGUMENTS` is exactly `ultracode`, run:

`bash ~/.claude/smart-toggle.sh effort ultracode`

If `$ARGUMENTS` is exactly `off`, run:

`bash ~/.claude/smart-toggle.sh effort off`

Print the command output verbatim. Do nothing else.
